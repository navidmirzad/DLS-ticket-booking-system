import Event from "../model/Event.js";

export const createEvent = async(event) => {
    
}

export const deleteEvent = async(event) => {
    
}

export const updateEvent = async(event) => {
    const existingEvent = Event.findById(event.id);

}